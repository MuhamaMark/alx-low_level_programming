#include "main.h"
#include<stdio.h>
#include<stdlib.h>
#include<string.h>
/**
 * _strlen - a function that prints the length of a string
 * @s: being parameter
 *
 * Return: 0 success
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);

	return (len);
}
