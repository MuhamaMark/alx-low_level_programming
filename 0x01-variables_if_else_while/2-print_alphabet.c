#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry Point
 * Return : always 0 (success)
 */
void main
{
	char ch;

	for(ch = ‘a’; ch <= ‘z’; ch++)
		putchar(ch);				  
		putchar (‘\n’);
	Return (0);
}
