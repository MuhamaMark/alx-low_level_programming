#include "main.h"
#include<string.h>
#include<stdio.h>
/**
 * _strchr -> function that locates a character in a string
 * @s: param1
 * @c: param2
 * Return: string
 */
char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
